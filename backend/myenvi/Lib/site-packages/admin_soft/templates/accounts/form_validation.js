// form_validation.js

if ("{{ form.username.errors }}" !== "") {
    alert("{{ form.username.errors|join:'. ' }}");
}
if ("{{ form.password.errors }}" !== "") {
    alert("{{ form.password.errors|join:'. ' }}");
}

if (user.is_authenticated){
    var msg = "You are authenticated as {{ username }}, but are not authorized to access this page. Would you like to login to a different account ?";
    alert(msg);
}

if (form.errors){
    if (form.errors.items|length == 1){}
        alert("Please correct the error below.");
    } else{
        alert("Please correct the errors below.");
    }
}

for (error in form.non_field_errors){
    alert("{{ error|clean_text }}");
}
